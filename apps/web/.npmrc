# HACK: Prevents Next.js from spawning "npm config get registry" in workspace
# context, which fails with ENOWORKSPACES in npm 10. Remove once Next.js is
# upgraded to a version that passes --no-workspaces in its internal npm call.
workspaces=false
